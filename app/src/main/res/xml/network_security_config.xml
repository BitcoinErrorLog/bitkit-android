<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration for Bitkit Android.
    
    Enforces HTTPS for all connections except:
    - localhost (for E2E testing with local regtest)
    - 10.0.2.2 (Android emulator host)
    
    See: https://developer.android.com/privacy-and-security/security-config
-->
<network-security-config>
    <!-- Default: Require HTTPS for all connections -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    
    <!-- Allow cleartext for local development/testing only -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>

